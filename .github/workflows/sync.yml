# This workflow is disabled since the main repository is private
# Documentation will be maintained manually in this public repository

# name: Sync Documentation from Main Repository
# 
# on:
#   schedule:
#     # Run every day at 2 AM UTC
#     - cron: '0 2 * * *'
#   workflow_dispatch:
# 
# jobs:
#   sync:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout docs repository
#         uses: actions/checkout@v4
#         with:
#           token: ${{ secrets.GITHUB_TOKEN }}
#           fetch-depth: 0
# 
#       - name: Checkout main repository
#         uses: actions/checkout@v4
#         with:
#           repository: radicalbit/radicalbit-ai-gateway
#           token: ${{ secrets.MAIN_REPO_TOKEN }}
#           path: main-repo
# 
#       - name: Sync documentation files
#         run: |
#           # Copy documentation files from main repository
#           cp main-repo/docs/README.md docs/technical-overview.md
#           cp main-repo/gateway/README.md docs/gateway-reference.md
#           cp main-repo/demo/README.md docs/demo-guide.md
#           
#           # Copy example configurations (remove secrets)
#           mkdir -p static/examples
#           cp main-repo/demo/config.yaml static/examples/demo-config.yaml
#           cp main-repo/gateway-config.yaml static/examples/basic-config.yaml
#           
#           # Remove sensitive information from config files
#           sed -i 's/api_key: !secret.*/api_key: "YOUR_API_KEY_HERE"/g' static/examples/*.yaml
#           sed -i 's/!secret.*/"YOUR_SECRET_HERE"/g' static/examples/*.yaml
# 
#       - name: Commit changes
#         run: |
#           git config --local user.email "action@github.com"
#           git config --local user.name "GitHub Action"
#           git add .
#           if git diff --staged --quiet; then
#             echo "No changes to commit"
#           else
#             git commit -m "Sync documentation from main repository [skip ci]"
#             git push
#           fi
